import styled from "styled-components";

import {dekstop} from "../../../styles/variables";
import {FlexStyle} from "../../../styles/Flex";


export const MainFlex = styled(FlexStyle)`
    margin: auto;
    max-width: ${dekstop};
    background: blue;
`;

